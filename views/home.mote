<div class="wrapper">
  <ul class="homepage">
    % posts.each do |post|
      <a href="/post/{{post.id}}"><li><span>{{post.date.strftime('%d %b %Y')}}</span>    {{post.title}}</li></a>
    % end
  </ul>
</div>