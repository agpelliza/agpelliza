<a href="{{post.id}}" id="{{hover}}">
  <div class="arrow-container">
    <h3>
      <small>{{post.date.strftime('%d %b %Y')}}</small> 
      {{post.title}}
    </h3>
    <div class="hover-box">
      <img src="/img/{{image}}" />
    </div>
  </div>
</a>